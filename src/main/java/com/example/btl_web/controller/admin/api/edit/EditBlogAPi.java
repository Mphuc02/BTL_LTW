package com.example.btl_web.controller.admin.api.edit;

public class EditBlogAPi {

    //            String cookieValue = null;
    //            Cookie[] cookies = req.getCookies();
    //            if (cookies != null) {
    //                for (Cookie cookie : cookies) {
    //                    if ("JSESSIONID".equals(cookie.getName())) {
    //                        cookieValue = cookie.getValue();
    //                        // Thực hiện các hành động khác với sessionId đã lấy được
    //                        break;
    //                    }
    //                }
    //            }
    //
    //            System.out.println("chuan bi");
    //            Gson gson = new Gson();
    //            String blogJson = gson.toJson(createBlog);
    //
    //            String url = "http://localhost:8080/api-blog";
    //            HttpClient httpClient = HttpClientBuilder.create().build();
    //            HttpPost request = new HttpPost(url);
    //            StringEntity params = new StringEntity(blogJson);
    //            request.addHeader("content-type", "application/json");
    //            request.setHeader("Cookie", "JSESSIONID=" + cookieValue);
    //            request.setEntity(params);
    //            HttpResponse response = httpClient.execute(request);
    //            HttpEntity entity = response.getEntity();
    //            String responseString = EntityUtils.toString(entity, "UTF-8");
}
